version: '3.8'

services:
  arya-registry:
    build: .
    image: arya-registry:latest
    ports:
      - "8761:8761"
    environment:
      - SERVER_PORT=8761
      - SPRING_PROFILES_ACTIVE=default
    restart: unless-stopped
